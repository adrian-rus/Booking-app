<%= form_for([@zone, @booking]) do |f| %>
  <p>
    <%= f.label 'start_time', 'Start time' %>
    <%= f.datetime_select :start_time, { minute_step: 15 } %>
  </p>
  <p>
    <%= f.label 'duration', 'Length of booking in hours' %>
    <%= f.number_field 'duration', min: 1 %>
  </p>
  <%= f.submit 'Submit' %>
<% end %> 
