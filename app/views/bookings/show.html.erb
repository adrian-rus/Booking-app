<br>
<div class="container">
 <div class="panel panel-success">
<div class="panel-heading">
 <h1>Booking Details</h1>
 </div>

 
 <div class="panel-body">
  
  <p>Your booking is confirmed for</p>
 
  <p><strong><%= @zone.name %> - <%= @zone.description %> </strong></p>
 
 <p>  Starts on: 
  <strong> <%= @booking.start_time.strftime('%e %b %Y') %>
  @ <%= @booking.start_time.strftime('%H:%M%p') %> </strong>
  
  and ends @  <%= (@booking.calculate_end_time + 60).strftime('%H:%M%p') %>
  </p>
  
  <p>Your have booked <%= @zone.name %> for 
   <%= @booking.duration %> hours 
  </p>

 <p>and the total cost is: </p>
  
  
  <p> <%= icon('euro', class:'strong')%> <%= (@booking.calculate_cost) %> </p>
 
 </div>
 
 <div class="panel-footer">
<%= link_to 'Edit booking', edit_zone_booking_path(@zone, @booking), class:'btn btn-warning' %> 
<%= link_to '<< Back to all bookings', zone_bookings_path(@zone), class:'btn btn-success' %>
</div>

 </div>
</div>

